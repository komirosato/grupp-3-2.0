import 'package:flutter/material.dart';
import 'package:template/widgets.dart';

class calculatorTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [
          headerMenu("Calculator"),
        ],
      ),
    );
  }
}

